properties:
  id: products
  name: Products
transformations:
  filter:
    expressionStatement: ${CategoryID} < 20
    alias:
      - name: CategoryID
        value: categoryid
  columnTransformations:
    - action: KEEP
      columnName: UnitsInStock
      newColumnName: Units_In_Stock
sourceTable:
  schema: '{{task.replication-41cx:null$_$dbo.schema}}'
  type: TABLE
  name: Products
